export enum enums {
  // baseUrl = "http://localhost:4000",
  // wsUrl = "localhost:4000",
  baseUrl = "https://server.dankner.be:4000",
  wsUrl = "server.dankner.be:4000",

  defaultHost = "chooseforme",
  dontChangeHost = "dontChangHost",
  noConnection = "No Internet Connection"
}
